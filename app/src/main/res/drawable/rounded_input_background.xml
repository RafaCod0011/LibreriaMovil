<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Fondo -->
    <solid android:color="#FFFFFF"/>

    <!-- Bordes redondeados -->
    <corners android:radius="12dp"/>

    <!-- Borde -->
    <stroke
        android:width="2dp"
        android:color="#6FB46F"/>

    <padding
        android:left="12dp"
        android:right="12dp"
        android:top="10dp"
        android:bottom="10dp"/>

</shape>